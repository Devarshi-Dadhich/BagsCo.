<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Profile | Scatch</title>
    <link rel="stylesheet" href="/stylesheets/profile.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <style>
        .profile-container {
            max-width: 480px;
            margin: 3rem auto;
            background: #fff;
            border-radius: 24px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.06);
            padding: 2.5rem 2rem 2rem 2rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1.2rem;
        }
        .profile-avatar {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            background: #dfe6e9;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            color: #636e72;
            margin-bottom: 1rem;
        }
        .profile-info {
            width: 100%;
            display: flex;
            flex-direction: column;
            gap: 0.7rem;
        }
        .profile-info label {
            font-weight: 600;
            color: #636e72;
        }
        .profile-info span {
            font-size: 1.1rem;
            color: #222;
        }
        .view-cart-btn {
            margin-left: 35vh;
            margin-top: 1rem;
            padding: 0.5rem 1.5rem;
            background: #00b894;
            color: #fff;
            border-radius: 24px;
            font-size: 1rem;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: background 0.2s;
        }
        .edit-btn {
            margin-top: 1.5rem;
            padding: 0.7rem 2rem;
            background: #0984e3;
            color: #fff;
            border-radius: 24px;
            font-size: 1.1rem;
            font-weight: 600;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: background 0.2s;
        }
        .edit-btn:hover {
            background: #74b9ff;
            color: #222;
        }
        @media (max-width: 600px) {
            .profile-container {
                padding: 1rem;
                border-radius: 12px;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">Scatch</div>
        <nav>
            <a href="/">Home</a>
            <a href="/products">Shop</a>
            <a href="/vendors/apply">Contact</a>
            <a href="/users/logout" class="btn">Logout</a>
        </nav>
    </header>
    <main>
        <div class="profile-container">
            <div class="profile-avatar">
                <% if (user.avatar) { %>
                    <img src="<%= user.avatar %>" alt="Avatar" style="width:100%;height:100%;border-radius:50%;">
                <% } else { %>
                    <%= user.name ? user.name[0].toUpperCase() : "U" %>
                <% } %>
            </div>
            <div class="profile-info">
                <div>
                    <label>Name:</label>
                    <span><%= user.name %></span>
                </div>
                <div>
                    <label>Email:</label>
                    <span><%= user.email %></span>
                </div>
                <div>
                    <label>Contact:</label>
                    <span><%= user.contact %></span>
                </div>
                <div>
                    <label>Cart:</label>
                    <span><%= user.cart %></span>
                    <button class="view-cart-btn"><a href="/users/cart/<%= user._id %>" style="text-decoration: none; color: #fff;">View Cart</a></button>
                </div>
            </div>
            <a href="/users/edit" class="edit-btn">Edit Profile</a>
        </div>
    </main>
    <footer>
        &copy; <%= new Date().getFullYear() %> Scatch. All rights reserved.
    </footer>
</body>
</html>